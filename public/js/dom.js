"use strict"
var navbarLinkElements = documennt.getElementByTagName('a');

var delay = 2000;
setTimeout(function () {
	for (var i = 0; i<navbarLinkElements.length; i ++;){
		consol.log(navbarLinkElements[i]);
	    var link = ""
	    navbarElements[i].setAttribute("href", link);
	}
	//navbarLinkElements.forEach(function(){element
	//	consol.long(element);
	//;



	//navbarElements.style.color='red';
}, delay);

//setTimeout( whats going to happen,after how long)
